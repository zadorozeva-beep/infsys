{
  "name": "aismat",
  "version": "1.0.0",
  "private": true,
  "description": "AIS навчальних матеріалів — root launcher (backend + frontend)",
  "scripts": {
    "start": "concurrently -k -n backend,frontend -c blue.bold,green.bold \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev": "npm start",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "migrate": "npm --prefix backend run migrate",
    "seed": "npm --prefix backend run seed",
    "stop": "stop.bat"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
